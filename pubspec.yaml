name: diplom
description: "Система реабилитации пациентов с ИИ-аналитикой"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  postgres: ^3.0.0
  provider: ^6.0.5
  fl_chart: ^1.2.0
  intl: ^0.20.2
  google_fonts: ^8.0.2
  http: ^1.6.0
  google_generative_ai: ^0.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
